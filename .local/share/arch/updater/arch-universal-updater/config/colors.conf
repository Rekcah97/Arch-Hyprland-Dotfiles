# =============================================================================
# colors.conf - user-level color overrides
# =============================================================================
# These values are optional overrides layered on top of the active theme
# (see THEME in config.conf and the files in themes/). Leave a value empty
# to keep the theme's default. Colors are 256-color palette indices (0-255)
# or "" to inherit from the theme file.
# =============================================================================

OVERRIDE_ACCENT=""
OVERRIDE_SUCCESS=""
OVERRIDE_WARNING=""
OVERRIDE_ERROR=""
OVERRIDE_TEXT=""
OVERRIDE_BORDER=""
